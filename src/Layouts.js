import styled from 'styled-components'

const InnerLayout = styled.div`
    padding: 4rem 10rem;
`;

const SectionLayout = styled.section`
    padding: 4rem 0rem;
`;

export {InnerLayout, SectionLayout}